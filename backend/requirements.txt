# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Database & Authentication
supabase==2.0.3

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# Image Processing (Python 3.11 compatible)
pillow==10.4.0
opencv-python-headless==4.10.0.84

# Scientific Computing (Python 3.11 compatible)
numpy==1.26.4

# Document Generation & Text Analysis
reportlab>=4.0.7
pytesseract>=0.3.10
textstat>=0.7.3

# NOTE: PyTorch removed to fit within Render free tier memory limits (512MB)
# For full attention analysis with saliency maps, install locally:
# pip install torch==2.5.1+cpu torchvision==0.20.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu
# Also removed: pandas, scipy (not essential for core analysis)
